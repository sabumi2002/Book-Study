


# 긴 if 문의 리팩터링
```
-- 예제 변경전
```
여기서 **if문은 함수의 시작에만 배치** 규칙의 중대한 위반을 합니다. 코드 중간에 길게 else if 체인 구문이 있습니다. 따라서 우리가 가장 먼저 해야할 일은 else if 체인 구문을 독자적인 메서드로 추출하는 것입니다.
```
-- 예제 변경후
```


### 리팩터링 패턴: 메서드 전문화

#### 설명
이 리팩터링은 많은 프로그래머의 본능에 반하기에 좀 난해합니다. 프로그래머들은 일반화하고 재사용하려는 본능적인 욕구가 있지만 그렇게 하면 책임이 흐려지고 다양한 위치에서 코드를 호출할 수 있기 때문에 문제가 될 수 있습니다. 이 리팩터링 패턴은 이 효과를 반전시킵니다. 좀 더 전문화된 메서드는 더 적은 위치에서 호출되어 필요성이 없어지면 더 빨리 제거할 수 있습니다.

#### 절차
1. 전문화하려는 메서드를 복제합니다.
2. 메서드 중 하나의 이름을 새로 사용할 메서드의 이름으로 변경하고 전문화하려는 매개변수를 제거(또는 교체)합니다.
3. 매개변수 제거에 따라 메서드를 수정해서 오류가 없도록 합니다.
4. 이전의 호출을 새로운 것을 사용하도록 변경합니다.

#### 예제
```
-- 예제 변경전
```

```
-- 예제 변경후
```

### switch가 허용되는 유일한 경우
더 이상 동작하지 않는 열거형 인덱스를 사용해서 map을 생성하고 있다는 것입니다. 이러한 인덱스는 일반적으로 데이터베이스나 파일에 무엇인가를 저장하는데 사용합니다. 게임의 경우 객체보다 직렬화하기 쉽기 때문에 인덱스를 사용해서 파일에 레벨을 저장하는 것이 논리적일 수 있습니다. 실제로는 리팩터링을 위해 기존의 외부 데이터를 변경하는 것이 불가능한 경우가 많습니다. 따라서 전체 map 을 변경하는 대신 열거형 인덱스에서 새로운 클래스를 사용하도록 새로운 함수를 만드는 것이 좋습니다. 

```
-- 예제 변경전
```

```
-- 예제 변경후
```

>**타입스크립트에서는**
>열거형은 자바에서와 같이 클래스가 아닌 C#에서와 같은 숫자에 대한 명칭입니다. 따라서 숫자와 열거형 간의 변환이 필요하지 않으며 이전 코드와 같이 열거형 인덱스를 사용하면 됩니다.

### 규칙: switch를 사용하지 말 것


### if 제거하기


# 코드 중복 처리
### 인터페이스 대신 추상 클래스를 사용할 수는 없을까?
### 규칙: 인터페이스에서만 상속받을 것
### 클래스에 있는 코드의 중복은 다 무엇일까?


# 복잡한 if 체인 구문 리팩터링


# 필요 없는 코드 제거하기
### 리팩터링 패턴: 삭제 후 컴파일하기
